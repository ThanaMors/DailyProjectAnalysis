# Gets certain daily stats for projects from Opensea + Looksrare

Outputs into data.txt file
**Put the project(s) slug & address into collections.json**

Install packages and then use **npm start** to run the script
